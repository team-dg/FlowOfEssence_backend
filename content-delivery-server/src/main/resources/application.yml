server:
    port: 6003

spring:
    profiles:
        default: local
    
    application:
        name: content-delivery-server
    
    config:
        import:
            - security/application-db.yml
    
    servlet:
        multipart:
            max-file-size: 5MB
            max-request-size: 5MB
