server:
    port: 6004

spring:
    profiles:
        default: local
    
    application:
        name: database-server
    
    config:
        import:
            - security/application-db.yml
    
    servlet:
        multipart:
            max-file-size: 5MB
            max-request-size: 5MB
